// @flow

export type Acceleration = {
  x: number,
  y: number,
  z: number,
};

export type Mouse = {
  x: number,
  y: number,
};

export type Screen = {
  width: number,
  height: number,
};

export type Touch = {
  x: number,
  y: number,
};
