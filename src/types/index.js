// @flow

export type Acceleration = {
  x: number,
  y: number,
  z: number,
};

export type MouseOrTouchPosition = {
  x: number,
  y: number,
};

export type Screen = {
  width: number,
  height: number,
};
