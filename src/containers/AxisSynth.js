// @flow

import * as React from 'react';

type Props = {};

export default class AxisSynth extends React.Component<Props> {
  render(): React.Node {
    return (
      <div>
        Init
      </div>
    );
  }
}
